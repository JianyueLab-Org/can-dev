# 开发者中心自己是网络上的一个 OAuth 应用。它是**机密客户端**（有后端，存得住
# 密钥），并且必须由 scripts/oauth-client.mjs 手工注册 —— 它要的 apps:manage
# 不在自助注册的名单里。注册命令见 README。
#
# 上游是两个地址：can-api 是 issuer，换令牌、userinfo、吊销和 /api/v1/dev/clients
# 都在它那儿；can-web 只剩同意页 /oauth/authorize，那是一个渲染给人看的页面，
# 没有跟着数据层搬进 Go。两者共用同一个数据库。
CAN_API_ORIGIN=https://api.ceruleanavi.net
CAN_WEB_ORIGIN=https://ceruleanavi.net
CAN_CLIENT_ID=can-dev
CAN_CLIENT_SECRET=

# 这个部署自己的对外地址。回调地址是 <PUBLIC_ORIGIN>/auth/callback，
# 且必须和注册时填的一字不差 —— 服务端是整串精确匹配，差一个斜杠都不行。
PUBLIC_ORIGIN=http://127.0.0.1:4322

# 会话 cookie 的加密密钥。32 字节以上的随机串：openssl rand -base64 32
SESSION_SECRET=
